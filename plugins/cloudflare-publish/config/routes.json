{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "cloudflare-publish.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/check",
      "handler": "cloudflare-publish.check",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/publish",
      "handler": "cloudflare-publish.publish",
      "config": {
        "policies": []
      }
    }
  ]
}
